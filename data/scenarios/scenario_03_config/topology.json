{
  "services": {
    "api-gateway": {
      "port": 8080,
      "depends_on": ["auth-service", "user-service"],
      "status": "healthy",
      "description": "Nginx-based API gateway, routes requests to backend services and delegates auth to auth-service"
    },
    "auth-service": {
      "port": 8083,
      "depends_on": ["redis"],
      "status": "degraded",
      "description": "Handles authentication via JWT tokens and session validation using Redis"
    },
    "user-service": {
      "port": 8081,
      "depends_on": ["postgres"],
      "status": "healthy",
      "description": "Handles user profile and account operations"
    },
    "postgres": {
      "port": 5432,
      "depends_on": [],
      "status": "healthy",
      "description": "PostgreSQL database for user and application data"
    },
    "redis": {
      "port": 6379,
      "depends_on": [],
      "status": "healthy",
      "description": "Redis instance for session storage and caching"
    }
  }
}
